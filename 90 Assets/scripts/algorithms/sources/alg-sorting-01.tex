\begin{tikzpicture}[font=\small]
  % ---------- merge sort recursion tree ----------
  \begin{scope}
    \node[anchor=south, font=\small] at (5.2,4.2){归并排序的递归树：每层总工作量 $O(n)$，共 $O(\log n)$ 层};

    % level 0
    \node[blk, minimum width=8.4cm, minimum height=0.6cm, fill=algblue!14, draw=algblue] at (5.2,3.4) {$n$};
    % level 1
    \foreach \x in {2.1,8.3}
      \node[blk, minimum width=4.1cm, minimum height=0.6cm, fill=algblue!12, draw=algblue] at (\x,2.5) {$n/2$};
    % level 2
    \foreach \x in {1.05,3.15,7.25,9.35}
      \node[blk, minimum width=1.95cm, minimum height=0.6cm, fill=algblue!10, draw=algblue] at (\x,1.6) {$n/4$};
    % level 3 (dots)
    \node[note] at (5.2,0.9) {$\vdots$};
    \node[blk, minimum width=8.4cm, minimum height=0.6cm, fill=algblue!8, draw=algblue] at (5.2,0.2) {$n$ 个大小为 1 的子数组};

    % right-hand annotations
    \foreach \y/\lb in {3.4/{$1\times n=n$}, 2.5/{$2\times n/2=n$}, 1.6/{$4\times n/4=n$}, 0.2/{$n\times 1=n$}}
      \node[note, anchor=west] at (10.9,\y){\lb};

    \draw[{Stealth[length=4pt]}-{Stealth[length=4pt]}, alggray] (-0.5,3.4) -- (-0.5,0.2);
    \node[note, anchor=east, align=right] at (-0.6,1.8){$\log_2 n$\\ 层};

    \node[note, anchor=north, align=center] at (5.2,-0.5)
      {总代价 $=$ 层数 $\times$ 每层代价 $=O(n\log n)$。\;
       递归式 $T(n)=2T(n/2)+O(n)$ 的主定理情形 2。};
  \end{scope}

  % ---------- comparison of the three ----------
  \begin{scope}[yshift=-3.4cm]
    \node[cell, fill=alglight!25, minimum width=2.3cm] at (0,0) {算法};
    \foreach \jj/\nm in {0/{最坏时间}, 1/{额外空间}, 2/{稳定}, 3/{原地}}
      \node[cell, fill=alglight!25, minimum width=2.2cm] at (2.25+\jj*2.2,0) {\nm};
    \node[cell, fill=alglight!25, text width=3.4cm, align=center] at (11.65,0) {关键思想};

    \foreach \rw/\alg/\tm/\sp/\st/\ip/\idea in {
      1/{选择排序}/{$O(n^2)$}/{$O(1)$}/{否}/{是}/{每轮选最大放到末尾},
      2/{插入排序}/{$O(n^2)$}/{$O(1)$}/{是}/{是}/{把新元素插进已排好的前缀},
      3/{归并排序}/{$O(n\log n)$}/{$O(n)$}/{是}/{否}/{分治：拆开、排好、合并}}{
      \node[cell, fill=alglight!12, minimum width=2.3cm] at (0,-\rw*0.72) {\alg};
      \node[cell, minimum width=2.2cm] at (2.25,-\rw*0.72) {\tm};
      \node[cell, minimum width=2.2cm] at (4.45,-\rw*0.72) {\sp};
      \node[cell, minimum width=2.2cm] at (6.65,-\rw*0.72) {\st};
      \node[cell, minimum width=2.2cm] at (8.85,-\rw*0.72) {\ip};
      \node[cell, text width=3.4cm, align=left, font=\footnotesize] at (11.65,-\rw*0.72) {\idea};
    }

    \node[note, anchor=north, align=center] at (5.5,-3.0)
      {比较排序的下界是 $\Omega(n\log n)$：$n!$ 种可能排列，每次比较最多把候选减半，
       决策树高度至少 $\log_2(n!)=\Theta(n\log n)$。\\
       想更快就得\textbf{放弃比较}这个手段 —— 那是下一讲线性时间排序的入口。};
  \end{scope}
\end{tikzpicture}
