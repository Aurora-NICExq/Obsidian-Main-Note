
#### `static`修饰的初始化
```c
void f() {
    static int x = 0;
    printf("%d", x++);
}

int main() {
    f();
    f();
    return 0;
}
```
1. `static` 变量存储在全局数据区，生命周期贯穿整个程序。
    
2. 初始化 `x = 0` **只在第一次调用时执行一次**。
    
3. 第一次调用：打印 0，x 变为 1。
    
4. 第二次调用：保留旧值 1，打印 1，x 变为 2。